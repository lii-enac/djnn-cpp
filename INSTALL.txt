We provide a Makefile that should work on Mac OS X and Linux platforms

Known dependencies:

Linux/Ubuntu:
  sudo apt install libboost-thread-dev libexpat1-dev libcurl4-openssl-dev libqt5-base

Mac OS X
  brew install boost qt5
 
On both platforms:
  ivy bus -> https://www.eei.cena.fr/products/ivy/

